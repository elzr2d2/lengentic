node_modules/
dist/
build/
.next/
coverage/
pnpm-lock.yaml
**/generated/
**/migrations/

# Generated by Next on every build, and gitignored. Formatting it just makes it dirty again.
next-env.d.ts

# Copied in verbatim as the reference document. Reformatting would make every diff against
# the original unreadable.
MVP_PLAN.md

# Emitted by `pnpm oracle md`. Hand-formatting a generated file only makes the next
# generation a spurious diff.
docs/PROJECT_STATUS.md

# Machine-local and gitignored — rewritten whenever a permission is granted. A file git
# refuses to track cannot be allowed to fail a shared gate.
.claude/settings.local.json
