#!/usr/bin/env sh

set -e

# Staged-scope gate ladder — secrets, format+re-stage, lint, affected-package
# typecheck/test, harness selftests when the harness is staged. gates:full moved
# to the phase gate and CI (harness-throughput item 5).
pnpm tsx scripts/precommit.ts
